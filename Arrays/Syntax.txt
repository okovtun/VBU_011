	
	type name;	//—интаксис объ€влени€ переменной
	type name[SIZE];//—интаксис объ€влени€ массива
type - тип элементов массива: bool, char, int, short, float, double.
name - им€ массива.
SIZE - количество элементов массива, может быть задано только целочисленным константным значением.

	const int SIZE = 5;
	int arr[SIZE];

for, 
счетчик 'i' - можно использовать как номер элемента.

for(int i = 0; i < SIZE; i++)
{
    cout << arr[i] << tab;
}
cout << endl;